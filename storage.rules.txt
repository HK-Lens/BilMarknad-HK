rules_version = '2';

/*
  File: storage.rules.txt
  Project: VORQ Fordon / bilmarknad-hk
  File version: vorq-fordon-storage-rules-delete-cascade-2026-06-07
  Last reviewed/updated: 2026-06-07 15:55 Europe/Berlin
  Status: Allows ad owners/admin to delete uploaded vehicle images from Firebase Storage.
  Storage path used by sell.html: cars/{sellerId}/{carId}/{fileName}
*/

service firebase.storage {
  match /b/{bucket}/o {
    function signedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      return signedIn()
        && (
          request.auth.token.admin == true
          || request.auth.token.role == "admin"
          || (
            request.auth.token.email == "info@vq-dx.com"
            && request.auth.token.email_verified == true
          )
        );
    }

    match /cars/{sellerId}/{carId}/{fileName} {
      allow read: if true;
      allow create, update, delete: if signedIn()
        && (
          request.auth.uid == sellerId
          || isAdmin()
        );
    }

    match /{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
